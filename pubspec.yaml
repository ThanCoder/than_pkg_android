name: than_pkg_android
description: "A new Flutter plugin project."
version: 0.0.1
homepage: https://github.com/ThanCoder/than_pkg_android.git
repository: https://github.com/ThanCoder/than_pkg_android.git

environment:
  sdk: ^3.12.0
  flutter: ">=3.3.0"

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
flutter:
  plugin:
    platforms:
      android:
        package: com.example.than_pkg_android
        pluginClass: ThanPkgAndroidPlugin
